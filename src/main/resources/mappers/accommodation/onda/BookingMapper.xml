<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.stay.accommodation.onda.mapper.BookingMapper">
    <select id="getOrderByOrderID" resultType="BookingDto">
        SELECT
            C.intCondoID,
            C.accomm_id,
            B.StartDay,
               EndDay,

        FROM tbl_Booking B WITH(NOLOCK)
            JOIN test_condo C WITH(NOLOCK) ON B.intCondoID = C.intCondoID
        WHERE OrderID = #{orderID}
    </select>

</mapper>